{
  "name": "grand-tensor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev.ts",
    "build": "tsc --noEmit && bun build ./index.html --outdir=dist --minify --public-path=./",
    "preview": "bun run preview.ts"
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2"
  }
}
